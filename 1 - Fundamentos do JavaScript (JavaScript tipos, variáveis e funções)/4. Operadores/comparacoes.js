// == (comparação implícita) compara o valor, retorna true ou false

const numero = 5;
const texto = "5";

console.log(numero == texto);

// === (comparação explícita) compara valores e tipos
Number();
String();
console.log(numero === texto);

// typeof para descobrir o tipo da variável
console.log(typeof numero);
console.log(typeof texto);

// == só compara o valor
// === compara o valor e o tipo de dado
